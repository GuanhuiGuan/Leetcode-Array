class Solution {
    public String reverseString(String s) {
        int n = s.length();
        if(n < 2){return s;}
        
        StringBuilder t = new StringBuilder();
        while(n > 0){
            t.append(s.charAt(n - 1));
            n--;
        }
        String r = t.toString();
        return r;
    }
}
